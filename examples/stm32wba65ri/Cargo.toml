[package]
name = "embedded-nn-stm32wba65ri"
version = "0.1.0"
edition = "2024"
publish = false

[[bin]]
name = "embedded-nn-stm32wba65ri"
path = "src/main.rs"

[[bin]]
name = "hil_agent"
path = "src/bin/hil_agent.rs"
required-features = ["hil-usb"]

[[bin]]
name = "data_collector"
path = "src/bin/data_collector.rs"

[features]
default = []
hil-usb = ["dep:embassy-usb", "dep:embedded-nn-live"]

[dependencies]
cortex-m = { version = "0.7.7", features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = "0.7.5"
defmt = "0.3.10"
defmt-rtt = "0.4.1"
embedded-hal = "1.0"
embedded-hal-async = "1.0"
libm = "0.2"
embassy-stm32 = { git = "https://github.com/embassy-rs/embassy", branch = "main", features = ["defmt", "memory-x", "stm32wba65ri", "time-driver-any", "exti"] }
embassy-executor = { git = "https://github.com/embassy-rs/embassy", branch = "main", features = ["platform-cortex-m", "executor-thread", "defmt"] }
embassy-time = { git = "https://github.com/embassy-rs/embassy", branch = "main", features = ["defmt"] }
embassy-sync = { git = "https://github.com/embassy-rs/embassy", branch = "main", features = ["defmt"] }
embassy-usb = { git = "https://github.com/embassy-rs/embassy", branch = "main", features = ["defmt"], optional = true }
embedded-nn = { path = "../../crates/embedded-nn", default-features = false, features = ["libm", "dsp"] }
embedded-nn-live = { path = "../../crates/embedded-nn-live", default-features = false, optional = true }
embedded-nn-macros = { path = "../../crates/embedded-nn-macros" }
panic-probe = { version = "0.3.2", features = ["print-defmt"] }
static_cell = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
