[workspace]
resolver = "2"
members = [
    "crates/embedded-nn",
    "crates/embedded-nn-compiler",
    "crates/embedded-nn-codegen",
    "crates/embedded-nn-macros",
    "crates/embedded-nn-live",
    "crates/embedded-nn-cli",
    "crates/embedded-nn-studio",
    "crates/embedded-nn-tflite",
    "crates/embedded-nn-train",
    "crates/embedded-nn-litert-plugin",
]
exclude = [
    "examples/qemu-lm3s6965",
    "examples/stm32wba65ri",
    "examples/rp2040-pico",
    "examples/embassy-rp2040",
    "examples/keyword-spotting",
    "examples/vibration-anomaly",
    "examples/imu-gesture",
    "examples/subbyte-quantization",
]

[workspace.package]
version = "0.2.0"
edition = "2024"
rust-version = "1.98"
authors = ["Gerzain Mata <leftger@gmail.com>"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/leftger/embedded-nn"
readme = "README.md"

[workspace.dependencies]
embedded-nn = { path = "crates/embedded-nn", default-features = false }
embedded-nn-compiler = { path = "crates/embedded-nn-compiler" }
embedded-nn-codegen = { path = "crates/embedded-nn-codegen" }
embedded-nn-macros = { path = "crates/embedded-nn-macros" }
embedded-nn-live = { path = "crates/embedded-nn-live" }
embedded-nn-tflite = { path = "crates/embedded-nn-tflite" }
embedded-nn-train = { path = "crates/embedded-nn-train" }

libm = { version = "0.2.11" }
serde = { version = "1.0", default-features = false, features = ["derive"] }
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }
thiserror = "2.0"
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
clap = { version = "4.5", features = ["derive"] }
criterion = { version = "0.5" }
embedded-dsp = { version = "0.2.2", default-features = false, features = ["libm"] }
flatbuffers = "25.12"

eframe = { version = "0.31", default-features = true }
egui_extras = { version = "0.31" }
rfd = "0.15"
nusb = "0.2"
